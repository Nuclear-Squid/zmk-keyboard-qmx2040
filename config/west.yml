manifest:
  remotes:
    - name: Nuclear-Squid
      url-base: https://github.com/Nuclear-Squid
  projects:
    # We use my personal fork of the project as it has a couple patches that
    # aren’t yet merged upstream that we rely on
    - name: zmk
      remote: Nuclear-Squid
      revision: fancy_tap_dances
      import: app/west.yml
  self:
    path: config
